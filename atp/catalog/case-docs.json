{
  "TC-API-ACT-001": {
    "summary": "Entity activity",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-001": {
    "summary": "Cross-org contact admin",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flows?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flows?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-002": {
    "summary": "Cross-org org admin",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/automation-rules?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/automation-rules?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-003": {
    "summary": "CRUD + toggle + preview",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/processes?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/processes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-004": {
    "summary": "CRUD + designer + test + executions",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-005": {
    "summary": "Approval inbox + decide",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-006": {
    "summary": "CRUD + health/metrics/bottlenecks",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-007": {
    "summary": "List/import templates",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-008": {
    "summary": "Automation context (read-only)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-009": {
    "summary": "App flow visibility",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-010": {
    "summary": "Batch automation check",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-011": {
    "summary": "Notification analytics",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-012": {
    "summary": "Public demo request",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-013": {
    "summary": "Admin demo management",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-014": {
    "summary": "Instance lifecycle",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-015": {
    "summary": "Metrics aggregation",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-016": {
    "summary": "weekly`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Manual digest trigger"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ADM-017": {
    "summary": "Parser config (platform admin)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/admin/business-flow-templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-001": {
    "summary": "GET/PUT `/config/me` — user booking config",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/PUT /config/me`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/PUT /config/me",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-002": {
    "summary": "GET `/config/pages` — booking pages list",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/appointments/config/pages`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/appointments/config/pages",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-003": {
    "summary": "GET `/config/slug-available` — slug check",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/appointments/config/slug-available?slug=atp-test`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/appointments/config/slug-available?slug=atp-test",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-004": {
    "summary": "GET/PUT `/config/user/:userId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/appointments/config/me`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/appointments/config/me",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-005": {
    "summary": "GET/PUT `/config` — org config",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/appointments/config`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/appointments/config",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-006": {
    "summary": "Google calendar connect start + callback",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-007": {
    "summary": "Microsoft calendar connect start + callback",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-008": {
    "summary": "DELETE calendar disconnect",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-009": {
    "summary": "CRUD `/config/team*` — team booking pages",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-010": {
    "summary": "GET `/stats`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/appointments/stats`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/appointments/stats",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-APT-011": {
    "summary": "GET/POST `/events/:id/*` — guest-link, reschedule, cancel, complete, no-show",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-001": {
    "summary": "Current auditor profile",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-002": {
    "summary": "Audit org context",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-003": {
    "summary": "Audit app health",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-004": {
    "summary": "Findings list",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-005": {
    "summary": "Responses list",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-006": {
    "summary": "Linked events",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-007": {
    "summary": "Form for audit",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-008": {
    "summary": "Response detail",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-009": {
    "summary": "Approval workflow",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-010": {
    "summary": "Submit form",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-011": {
    "summary": "Audit org list",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-012": {
    "summary": "Update address",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-013": {
    "summary": "Create audit event",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-014": {
    "summary": "Check-in",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-015": {
    "summary": "Submit audit",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-016": {
    "summary": "Approve",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-017": {
    "summary": "Reject",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-018": {
    "summary": "Assignment list",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-019": {
    "summary": "Assignment detail",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-020": {
    "summary": "Timeline",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUD-021": {
    "summary": "Execution status",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/audit/assignments?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-001": {
    "summary": "Owner login returns a valid JWT and user profile",
    "howToRun": [
      "POST /api/auth/login with owner email/password from fixtures/personas.json.",
      "Assert HTTP 200 and a token in the response body.",
      "Token is cached for subsequent cases in the same run."
    ],
    "request": {
      "method": "POST",
      "path": "/api/auth/login",
      "auth": "None",
      "body": {
        "email": "<owner>",
        "password": "<password>"
      }
    },
    "expected": {
      "status": 200,
      "behavior": "JWT issued; user can call authenticated APIs."
    },
    "onFailure": {
      "typicalError": "401 Invalid credentials",
      "likelyCauses": [
        "Wrong password",
        "User disabled",
        "SUT auth misconfigured"
      ],
      "remediation": [
        "Set ATP_PERSONA_OWNER_EMAIL/PASSWORD in atp/.env",
        "Confirm user exists in LiteDesk tenant"
      ]
    }
  },
  "TC-API-AUTH-002": {
    "summary": "POST `/api/auth/login` invalid password",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-003": {
    "summary": "POST `/api/auth/login` rate limit exceeded (6th attempt/15min prod)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "429"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-004": {
    "summary": "POST `/api/auth/register` new org",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "201 + token"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-005": {
    "summary": "POST `/api/auth/register` duplicate email",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "4xx"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-006": {
    "summary": "GET `/api/users/profile` with Bearer token",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "200 refreshed user + org"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-007": {
    "summary": "GET `/api/users/profile` expired/invalid token",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-AUTH-008": {
    "summary": "GET `/api/auth/test-version`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `API`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "API",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "200 (public)"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BH-001": {
    "summary": "GET `/resolve` — resolve active hours",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /resolve`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /resolve",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BH-002": {
    "summary": "CRUD `/sets` — business hour sets",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/business-hours/sets`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/business-hours/sets",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BH-003": {
    "summary": "POST `/simulate` — simulate datetime in hours",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/business-hours/simulate`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/business-hours/simulate",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-BH-004": {
    "summary": "GET/POST `/kpis` — daily KPIs",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/business-hours/kpis`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/business-hours/kpis",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BH-005": {
    "summary": "CRUD `/holiday-calendars` + import",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/business-hours/holiday-calendars`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/business-hours/holiday-calendars",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BOX-001": {
    "summary": "CRUD mailbox",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `CRUD mailbox`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "CRUD mailbox",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BOX-002": {
    "summary": "Gmail OAuth start + callback",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/mailboxes?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BOX-003": {
    "summary": "Inbox sync run",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/mailboxes?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BOX-004": {
    "summary": "Inbound parser provision",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/mailboxes?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-BOX-005": {
    "summary": "Outbound Gmail SMTP send",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/mailboxes?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/create case"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-002": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/__CASE__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/__CASE__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-003": {
    "summary": "`/:id/status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/helpdesk/cases/__CASE__/status`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/helpdesk/cases/__CASE__/status",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Status transition"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-004": {
    "summary": "`/:id/reopen`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/helpdesk/cases/__CASE__/reopen`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/helpdesk/cases/__CASE__/reopen",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Reopen closed case"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-005": {
    "summary": "`/:id/activities`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/helpdesk/cases/__CASE__/activities`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/helpdesk/cases/__CASE__/activities",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Add activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-006": {
    "summary": "`/bulk/update`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/helpdesk/cases/bulk/update`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/helpdesk/cases/bulk/update",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Bulk update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-007": {
    "summary": "`/ingest/channel`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/helpdesk/cases/ingest/channel`.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/helpdesk/cases/ingest/channel",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Channel ingest"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-008": {
    "summary": "`/canned-responses`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/canned-responses`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/canned-responses",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Canned responses list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-009": {
    "summary": "`/analytics/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Analytics summary"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-010": {
    "summary": "`/analytics/trends`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Trend data"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-011": {
    "summary": "`/analytics/owners`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/analytics/owners`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/analytics/owners",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Owner metrics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-012": {
    "summary": "`/analytics/distribution`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/analytics/distribution`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/analytics/distribution",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Distribution"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-013": {
    "summary": "`/analytics/audit-export`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/analytics/audit-export`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/analytics/audit-export",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Audit export"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-014": {
    "summary": "`/:id/chat/session`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/__CASE__/chat/session`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/__CASE__/chat/session",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Live chat session"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-015": {
    "summary": "`/:id/chat/messages`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/__CASE__/chat/messages`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/__CASE__/chat/messages",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Chat messages"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-016": {
    "summary": "`/:id/chat/stream`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/helpdesk/cases/__CASE__/chat/stream`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/helpdesk/cases/__CASE__/chat/stream",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Chat SSE stream"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CASE-017": {
    "summary": "`/:id/chat/read`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/helpdesk/cases/__CASE__/chat/read`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/helpdesk/cases/__CASE__/chat/read",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Mark read"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CASE-018": {
    "summary": "`/:id/chat/typing`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/helpdesk/cases/__CASE__/chat/typing`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/helpdesk/cases/__CASE__/chat/typing",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Typing indicator"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CAT-001": {
    "summary": "`/variants/search`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Variant search"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-002": {
    "summary": "`/variants/:variantId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/catalog/price-books`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/catalog/price-books",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Variant detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-003": {
    "summary": "`/variants/:variantId/price-entries`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Price entries"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-004": {
    "summary": "`/variants/:variantId/bundle-components`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Bundle config"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-005": {
    "summary": "`/variants/:variantId/bundle-expand`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Expand bundle"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-006": {
    "summary": "`/price-books/resolve`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/catalog/price-books/resolve`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/catalog/price-books/resolve",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Resolve price"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CAT-007": {
    "summary": "`/price-books` (+ entries)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/catalog/price-books`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/catalog/price-books",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Price books"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CAT-008": {
    "summary": "`/categories` (+ attributes)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/catalog/categories`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/catalog/categories",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Categories"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-001": {
    "summary": "`/entity-types/:entity`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Entity types"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-002": {
    "summary": "`/lifecycles/:entityTypeKey`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/config-registry/lifecycles/people`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/config-registry/lifecycles/people",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Lifecycles"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-003": {
    "summary": "`/lifecycle-status-mappings/:lifecycleKey`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/config-registry/lifecycle-status-mappings/people_lifecycle`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/config-registry/lifecycle-status-mappings/people_lifecycle",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Status mappings"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-004": {
    "summary": "`/pipelines`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Pipelines"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-005": {
    "summary": "`/pipelines/:pipelineKey/stages`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/config-registry/pipelines/deals/stages`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/config-registry/pipelines/deals/stages",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Pipeline stages"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-006": {
    "summary": "`/compute-derived-status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/config-registry/compute-derived-status`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/config-registry/compute-derived-status",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Derived status compute"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CFG-007": {
    "summary": "`/configuration/:entity`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Entity configuration"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CFG-008": {
    "summary": "`/configuration`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Full configuration"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-001": {
    "summary": "`/inbox/stream`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "SSE inbox stream (token)"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-002": {
    "summary": "`/email/compose-preview`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/email/compose-preview?moduleKey=people&recordId=__RECORD__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/email/compose-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Compose preview"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-003": {
    "summary": "`/email/reply-to-preview`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/email/reply-to-preview?moduleKey=people&recordId=__RECORD__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/email/reply-to-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Reply-to preview"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-004": {
    "summary": "`/email`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Send outbound email"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-005": {
    "summary": "`/pipeline-metrics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Pipeline metrics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-006": {
    "summary": "`/pipeline-diagnostics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Diagnostics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-007": {
    "summary": "`/inbound/diagnostics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/inbound/diagnostics`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/inbound/diagnostics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Inbound diagnostics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-008": {
    "summary": "`/inbound/dead-letter`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/inbound/dead-letter`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/inbound/dead-letter",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Dead letter queue"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-009": {
    "summary": "`/inbound/dead-letter/:id/replay`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Replay dead letter"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-010": {
    "summary": "`/suppressions/stats`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Suppression stats"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-011": {
    "summary": "`/suppressions/:email`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Manage suppressions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-012": {
    "summary": "`/webhook-test/*`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/webhook-test/events`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/webhook-test/events",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Webhook simulation"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-013": {
    "summary": "`/threads`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Thread list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-014": {
    "summary": "`/threads/:threadId/messages`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/workspace-threads`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/workspace-threads",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Thread messages"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-015": {
    "summary": "`/workspace-threads`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/workspace-thread-ids`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/workspace-thread-ids",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Workspace threads"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-016": {
    "summary": "`/workspace-thread-ids`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/workspace-thread-counts`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/workspace-thread-counts",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Thread IDs"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-017": {
    "summary": "`/workspace-thread-counts`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Counts"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-018": {
    "summary": "`/templates`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Email templates"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-019": {
    "summary": "`/threads/bulk`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/templates`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Bulk thread actions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-020": {
    "summary": "`/threads/:threadId/view`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/communications/inbound/diagnostics`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/communications/inbound/diagnostics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Mark viewed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-021": {
    "summary": "`/threads/:threadId/done`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Mark done"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-022": {
    "summary": "`/threads/:threadId/snooze`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Snooze thread"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-023": {
    "summary": "`/threads/:threadId/assign`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Assign owner"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-024": {
    "summary": "`/threads/:threadId/tags`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Thread tags"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-025": {
    "summary": "`/:communicationId/create-task`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create task from email"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-026": {
    "summary": "`/:communicationId/create-case`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create case from email"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-027": {
    "summary": "`/attachments/download`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Download attachment"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-COM-028": {
    "summary": "`/upload`, `/upload-oci`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Upload attachment"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CSV-001": {
    "summary": "POST `/parse` — parse uploaded CSV",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CSV-002": {
    "summary": "POST `/staging` — stage rows",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CSV-003": {
    "summary": "POST `/check-duplicates/{contacts,people,deals,tasks,organizations}`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/csv/check-duplicates/contacts`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/csv/check-duplicates/contacts",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-CSV-004": {
    "summary": "POST `/import/{contacts,people,deals,tasks,organizations}` — queue/import",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-CSV-005": {
    "summary": "GET `/export/{contacts,people,deals,tasks,organizations}`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /export/{contacts,people,deals,tasks,organizations}`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /export/{contacts,people,deals,tasks,organizations}",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-001": {
    "summary": "`/dashboard/metrics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Dashboard KPIs"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-002": {
    "summary": "`/pipeline/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Pipeline summary"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-003": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/kanban data"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-004": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create deal"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-005": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Deal detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-006": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update deal"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-007": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `DELETE`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "DELETE",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Trash"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-008": {
    "summary": "`/:id/stage`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PATCH`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PATCH",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Pipeline stage change"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-009": {
    "summary": "`/:id/tags`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Tag management"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-010": {
    "summary": "`/:id/notes`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Add note"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-011": {
    "summary": "`/:id/notes/:noteId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Edit note"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-012": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-013": {
    "summary": "`/:id/description-versions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Version history"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-014": {
    "summary": "`/:id/description-versions/restore`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Restore description"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-015": {
    "summary": "`/:id/comments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Comment thread"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-016": {
    "summary": "`/:id/comments/:commentId/reactions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Reactions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-DEAL-017": {
    "summary": "`/:id/comments` + attachment",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/deals/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Comment with file"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-001": {
    "summary": "`/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Calendar summary"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List events"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-003": {
    "summary": "`/stats`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Event stats"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-004": {
    "summary": "`/export`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/events/export`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/events/export",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Export events"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-005": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/`.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Create event"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-006": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/events/__EVENT__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/events/__EVENT__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-EVT-007": {
    "summary": "`/bulk-delete`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/bulk-delete`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/bulk-delete",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Bulk delete"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-008": {
    "summary": "`/:id/notes`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/notes`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/notes",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Add note"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-009": {
    "summary": "`/:id/start`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/start`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/start",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Start execution"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-010": {
    "summary": "`/:id/check-in`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/check-in`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/check-in",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Geo check-in"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-011": {
    "summary": "`/:id/check-out`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/check-out`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/check-out",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Check-out"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-012": {
    "summary": "`/:id/submit-audit`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/submit-audit`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/submit-audit",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Submit audit form"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-013": {
    "summary": "`/:id/approve-audit`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/approve-audit`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/approve-audit",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Approve audit"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-014": {
    "summary": "`/:id/reject-audit`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/reject-audit`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/reject-audit",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Reject audit"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-015": {
    "summary": "`/:id/next-org`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/next-org`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/next-org",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Multi-org beat next"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-016": {
    "summary": "`/:id/orders`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/orders`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/orders",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Field sales orders"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-017": {
    "summary": "`/:id/complete`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/complete`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/complete",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Complete event"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EVT-018": {
    "summary": "`/:id/cancel`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/events/__EVENT__/cancel`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/events/__EVENT__/cancel",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Cancel event"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-EXEC-001": {
    "summary": "Execute action",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FILE-001": {
    "summary": "GET/POST files",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FILE-002": {
    "summary": "Download (optional auth)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/create forms"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-002": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/forms/__FORM__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/forms/__FORM__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-003": {
    "summary": "`/:id/duplicate`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/forms/__FORM__/duplicate`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/forms/__FORM__/duplicate",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Duplicate form"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-FRM-004": {
    "summary": "`/:id/enable-public`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/forms/__FORM__/enable-public`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/forms/__FORM__/enable-public",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Enable public slug"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-FRM-005": {
    "summary": "`/:id/link-event`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/forms/__FORM__/link-event`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/forms/__FORM__/link-event",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Link to event"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-FRM-006": {
    "summary": "`/:id/analytics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/forms/__FORM__/analytics`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/forms/__FORM__/analytics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Form analytics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-007": {
    "summary": "`/:id/kpis`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/forms/__FORM__/kpis`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/forms/__FORM__/kpis",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "KPI metrics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-008": {
    "summary": "`/:id/submit`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/forms/__FORM__/submit`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/forms/__FORM__/submit",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Internal submit"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-FRM-009": {
    "summary": "`/responses/all`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/forms/responses/all?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/forms/responses/all?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "All responses"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-010": {
    "summary": "`/:id/responses`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/forms/__FORM__/responses?limit=5`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/forms/__FORM__/responses?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Form responses list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-011": {
    "summary": "`/:id/responses/export`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Export responses"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-012": {
    "summary": "`/:id/responses/:responseId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Single response"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-013": {
    "summary": "`/:id/responses/:responseId/status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Status change"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-014": {
    "summary": "`/:id/responses/:responseId/corrective-action`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Corrective action"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-015": {
    "summary": "`/:id/responses/:responseId/verify`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Auditor verify"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-016": {
    "summary": "`/:id/responses/:responseId/approve`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Approve response"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-017": {
    "summary": "`/:id/responses/:responseId/reject`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Reject response"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-018": {
    "summary": "`/:id/responses/:responseId/archive`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Archive"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-019": {
    "summary": "`/:id/responses/:responseId/invalidate`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Invalidate"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-020": {
    "summary": "`/:id/responses/:responseId/restore`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Restore"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-021": {
    "summary": "`/:id/responses/:responseId/compare`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Compare versions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-022": {
    "summary": "`/:id/responses/:responseId/generate-report`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "PDF report"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-023": {
    "summary": "`/:id/responses/:responseId/generate-comprehensive-report`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Full report"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-024": {
    "summary": "`/:id/responses/:responseId/export-excel`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Excel export"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-FRM-025": {
    "summary": "`/organization/:organizationId/audits`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Audit forms list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create group"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List groups"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-003": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Group detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-004": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update group"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-005": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Delete group"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-006": {
    "summary": "`/:id/members`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Add members"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-007": {
    "summary": "`/:id/members`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Remove members"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-GRP-008": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Group activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-HEALTH-001": {
    "summary": "200",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-HEALTH-002": {
    "summary": "200 when DB up",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /health/ready`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /health/ready",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-HEALTH-003": {
    "summary": "Component status",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /health/status`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /health/status",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-HEALTH-004": {
    "summary": "Per controller rules",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /health/mailroom-metrics without token`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /health/mailroom-metrics without token",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-HEALTH-005": {
    "summary": "Internal auth",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-IMP-001": {
    "summary": "GET `/imports` — list jobs",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /imports`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /imports",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-IMP-002": {
    "summary": "GET `/imports/stats/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /imports/stats/summary`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /imports/stats/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-IMP-003": {
    "summary": "GET `/imports/:id` — job detail",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /imports/:id`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /imports/:id",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-IMP-004": {
    "summary": "GET `/imports/:id/records/:type` — imported rows",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET /imports/:id/records/:type`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET /imports/:id/records/:type",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-IMP-005": {
    "summary": "DELETE `/imports/:id` — cancel/delete job",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated DELETE request to `/api/imports/__IMPORT__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "DELETE",
      "path": "/api/imports/__IMPORT__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-INBOX-001": {
    "summary": "GET `/` unified inbox aggregation",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET / unified inbox aggregation`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET / unified inbox aggregation",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-001": {
    "summary": "`/statistics`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/items/statistics`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/items/statistics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Catalog stats"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-002": {
    "summary": "`/low-stock`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/items/low-stock`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/items/low-stock",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Low stock alert"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-003": {
    "summary": "`/type/:type`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/items/type/product`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/items/type/product",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Filter by type"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-004": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/create item"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-005": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-006": {
    "summary": "`/:id/stock`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Stock adjustment"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-007": {
    "summary": "`/:id/media`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Media gallery"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-008": {
    "summary": "`/:id/variants`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Variants"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-009": {
    "summary": "`/:id/link-deal`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Link to deal"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ITEM-010": {
    "summary": "`/:id/unlink-deal/:dealId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Unlink deal"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-001": {
    "summary": "`/people/quick-create`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/people/quick-create`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/people/quick-create",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Quick-create metadata"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Module definitions CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-003": {
    "summary": "`/system/:key`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "System module update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-004": {
    "summary": "`/:moduleKey/records/batch`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/modules/people/records/batch`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/modules/people/records/batch",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Bulk fetch"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-MOD-005": {
    "summary": "`/:moduleKey/records/:recordId/activity`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/people/records/__RECORD__/activity`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/people/records/__RECORD__/activity",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Activity feed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-006": {
    "summary": "`/:moduleKey/records/:recordId/comments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/people/records/__RECORD__/comments`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/people/records/__RECORD__/comments",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Comments"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-007": {
    "summary": "`/:moduleKey/records/:recordId/comment-attachments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Attachments"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-008": {
    "summary": "`/:moduleKey/records/:recordId/comments/:commentId/reactions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Reactions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-009": {
    "summary": "`/:moduleKey/tags/delete`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Bulk tag delete"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-010": {
    "summary": "`/:moduleKey/records/:recordId/neighbors`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/people/records/__RECORD__/neighbors`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/people/records/__RECORD__/neighbors",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Prev/next nav"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MOD-011": {
    "summary": "`/:moduleKey/records/:recordId/description-versions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/modules/people/records/__RECORD__/description-versions`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/modules/people/records/__RECORD__/description-versions",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Description versions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MRM-001": {
    "summary": "GET attachment download",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-MRM-002": {
    "summary": "POST chat ingest",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-001": {
    "summary": "SSE notification stream",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/notifications/stream`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/notifications/stream",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-002": {
    "summary": "List notifications",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/notifications/`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/notifications/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-003": {
    "summary": "Mark read",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/notifications?appKey=SALES&limit=10`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/notifications?appKey=SALES&limit=10",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-004": {
    "summary": "Mark all read",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/notifications/read-all`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/notifications/read-all",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-NOT-005": {
    "summary": "Dev simulate (non-prod)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/push/subscriptions`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/push/subscriptions",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-006": {
    "summary": "GET/PUT prefs",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOT-007": {
    "summary": "CRUD + toggle rules",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-NOTE-001": {
    "summary": "GET/POST notes",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Tenant org profile"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update org (owner)"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-003": {
    "summary": "`/stats`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Org statistics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-004": {
    "summary": "`/subscription`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Subscription state"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-005": {
    "summary": "`/subscription/upgrade`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Upgrade plan"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-006": {
    "summary": "`/subscription/cancel`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Cancel subscription"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-007": {
    "summary": "`/apps/enable`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Enable app on tenant"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-008": {
    "summary": "`/apps/disable`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Disable app"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-009": {
    "summary": "`/:id/apps/enable`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Enable app by id"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORG-010": {
    "summary": "`/:id/apps/disable`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Disable app by id"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGS-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create via surface API"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGS-002": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Read surface record"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGS-003": {
    "summary": "`/:id/editable`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Editable field projection"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGS-004": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Partial/full update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGS-005": {
    "summary": "`/:id/surface`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Full surface composition"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create company account"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List company accounts"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-003": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Account detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-004": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update account fields"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-005": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/v2/organization/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Soft-delete → trash"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-006": {
    "summary": "`/:id/surface`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Composed surface payload"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-007": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Activity history"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ORGV2-008": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Log manual activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-001": {
    "summary": "`/resolve-context`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Resolve create context"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-002": {
    "summary": "`/resolve-types`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "People type definitions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-003": {
    "summary": "`/resolve-quick-create`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Quick-create field projection"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-004": {
    "summary": "`/create`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create person with appKey"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-005": {
    "summary": "`/create`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Attach to existing by email"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-006": {
    "summary": "`/:id/attach`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Add SALES participation"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-007": {
    "summary": "`/:id/detach`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Remove app participation"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-008": {
    "summary": "`/:id/convert-lead-to-contact`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Lead → Contact lifecycle"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-009": {
    "summary": "`/:id/profile`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Composed profile"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-010": {
    "summary": "`/:id/update-core`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Core identity fields"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-011": {
    "summary": "`/:id/update-app-fields`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "participations.{appKey}"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-012": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List with filters/pagination"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-013": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Legacy create path"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-014": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Single record"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-015": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Full update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-016": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `DELETE`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "DELETE",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Soft-delete → trash"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-017": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Activity feed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-018": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Manual activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-019": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Blocked when dependencies exist"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PEO-020": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/people/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "`createdBy` update blocked by hook"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PREF-001": {
    "summary": "`/widget-layout`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST/GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST/GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Save/load dashboard widget layout"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PREF-002": {
    "summary": "`/metrics-config`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `POST/GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "POST/GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Save/load metrics config"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-001": {
    "summary": "`/me`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Portal user profile"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-002": {
    "summary": "`/org`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Portal org"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-003": {
    "summary": "`/health`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Health"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-004": {
    "summary": "`/audits`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Assigned audits"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-005": {
    "summary": "`/audits/:eventId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Audit detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-006": {
    "summary": "`/actions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Corrective actions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-007": {
    "summary": "`/actions/:actionId/evidence`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Upload evidence"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-008": {
    "summary": "`/cases`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Support cases"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-009": {
    "summary": "`/cases/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Case detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-010": {
    "summary": "`/cases/:id/reply`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Reply to case"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-011": {
    "summary": "`/mailroom/ingest`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Inbound mail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-012": {
    "summary": "`/mailroom/cases/:caseId/reply`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Mailroom reply"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-013": {
    "summary": "`/mailroom/attachments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Upload attachment"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-014": {
    "summary": "`/mailroom/conversations/:conversationId/attachments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List attachments"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-015": {
    "summary": "`/mailroom/messages/:messageId/attachments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Message attachments"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PRT-016": {
    "summary": "`/mailroom/attachments/:id/download`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/portal/cases?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Download"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PUSH-001": {
    "summary": "VAPID key (public)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PUSH-002": {
    "summary": "Web push subscribe",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-PUSH-003": {
    "summary": "Unsubscribe",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-001": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/create quote"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-002": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/PUT/DELETE`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/PUT/DELETE",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-003": {
    "summary": "`/:id/revisions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Revision history"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-004": {
    "summary": "`/:id/process-approvals`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/quotes/__QUOTE__/process-approvals`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/quotes/__QUOTE__/process-approvals",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Process approvals"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-005": {
    "summary": "`/:id/conversion`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/quotes/__QUOTE__/conversion`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/quotes/__QUOTE__/conversion",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Conversion status"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-006": {
    "summary": "`/:id/status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/quotes/__QUOTE__/status`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/quotes/__QUOTE__/status",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Status transition"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-007": {
    "summary": "`/:id/submit-for-approval`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/submit-for-approval`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/submit-for-approval",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Submit → locked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-008": {
    "summary": "`/:id/approve`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/approve`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/approve",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Approve quote"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-009": {
    "summary": "`/:id/reject`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/reject`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/reject",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Reject quote"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-010": {
    "summary": "`/:id/send-email`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/send-email`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/send-email",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Email to customer"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-011": {
    "summary": "`/:id/share`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/share`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/share",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Generate public token"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-012": {
    "summary": "`/:id/share/revoke`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/share/revoke`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/share/revoke",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Revoke token"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-013": {
    "summary": "`/:id/convert`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/convert`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/convert",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Convert to deal"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-014": {
    "summary": "`/:id/recalculate`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/recalculate`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/recalculate",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Recalc totals"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-015": {
    "summary": "`/:id/discounts`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/quotes/__QUOTE__/discounts`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/quotes/__QUOTE__/discounts",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Apply discounts"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-016": {
    "summary": "`/:id/revise`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/quotes/__QUOTE__/revise`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/quotes/__QUOTE__/revise",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "New revision"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-QTE-017": {
    "summary": "`/:id/sections`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/quotes/__QUOTE__/sections`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/quotes/__QUOTE__/sections",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Quote sections"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-018": {
    "summary": "`/:id/lines` (+ bundles, reorder)",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/quotes/__QUOTE__/lines`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/quotes/__QUOTE__/lines",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Line items"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-QTE-019": {
    "summary": "`/:id/documents`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/quotes/__QUOTE__/documents`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/quotes/__QUOTE__/documents",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "PDF documents"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-REL-001": {
    "summary": "`/link`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create link"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-REL-002": {
    "summary": "`/unlink`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Remove link"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-REL-003": {
    "summary": "`/links`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/relationships/links?moduleKey=people&recordId=__RECORD__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/relationships/links?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List links"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-REL-004": {
    "summary": "`/linkable-targets`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/relationships/linkable-targets?moduleKey=deals&recordId=__RECORD__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/relationships/linkable-targets?moduleKey=deals&recordId=__RECORD__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Picker targets"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-REL-005": {
    "summary": "`/record-context`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/relationships/record-context?moduleKey=people&recordId=__RECORD__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/relationships/record-context?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Record page widgets"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-001": {
    "summary": "`/modules`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Permission catalog for role editor"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-002": {
    "summary": "`/hierarchy`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Role tree"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-003": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List roles"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-004": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Role detail with appPermissions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-005": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create custom role"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-006": {
    "summary": "`/initialize`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Seed default roles"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-007": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update permissions matrix"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-008": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Delete non-system role"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-ROLE-009": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "System role → blocked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RPT-001": {
    "summary": "CRUD + run + export",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/reports`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/reports",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RPT-002": {
    "summary": "| TC-API-RPT-001–005 | /api/reports | CRUD + run + export |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/reports?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/reports?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RPT-003": {
    "summary": "| TC-API-RPT-001–005 | /api/reports | CRUD + run + export |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/reports?limit=1`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/reports?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RPT-004": {
    "summary": "| TC-API-RPT-001–005 | /api/reports | CRUD + run + export |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/reports?limit=1`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/reports?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RPT-005": {
    "summary": "| TC-API-RPT-001–005 | /api/reports | CRUD + run + export |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/reports?limit=1`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/reports?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-RSP-001": {
    "summary": "`/:responseId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Read-only response detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SCH-001": {
    "summary": "GET/POST `/` — list/create schedules",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST /`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST /",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SCH-002": {
    "summary": "GET `/:entityType/:entityId` — by entity",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/scheduling/deals/__DEAL__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/scheduling/deals/__DEAL__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SCH-003": {
    "summary": "GET/PUT/DELETE `/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/scheduling/__SCH__`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/scheduling/__SCH__",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SCH-004": {
    "summary": "PATCH `/:id/status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/scheduling/__SCH__/status`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/scheduling/__SCH__/status",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-SCH-005": {
    "summary": "PATCH `/:id/reschedule`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated PATCH request to `/api/scheduling/__SCH__/reschedule`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "PATCH",
      "path": "/api/scheduling/__SCH__/reschedule",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-SET-001": {
    "summary": "GET/PATCH core module configs",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `Core modules`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "Core modules",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-002": {
    "summary": "GET/PATCH/PUT status types",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/core-modules/organizations/status-types`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/core-modules/organizations/status-types",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-003": {
    "summary": "GET/PUT people-types",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/core-modules/people/people-types`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/core-modules/people/people-types",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-004": {
    "summary": "GET apps list + per-app config",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/applications`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/applications",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-005": {
    "summary": "GET/PUT quote defaults",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/quotes`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/quotes",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-006": {
    "summary": "CRUD assignment rules",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/automation/assignment-rules`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/automation/assignment-rules",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-007": {
    "summary": "GET/PUT subscription settings",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/subscriptions`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/subscriptions",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-008": {
    "summary": "CRUD + logo upload",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/organization`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/organization",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-009": {
    "summary": "GET/PUT security settings + activity log",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/security`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/security",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-010": {
    "summary": "GET/POST/PUT integration configs",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/integrations`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/integrations",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-011": {
    "summary": "GET/PUT SLA settings",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/applications/helpdesk/execution-settings`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/applications/helpdesk/execution-settings",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-012": {
    "summary": "POST recalculate",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/settings/applications/helpdesk/recalculate-slas`.",
      "Assert response status is HTTP 200 or 202 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/settings/applications/helpdesk/recalculate-slas",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        202,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 202 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 202, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 202 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-SET-013": {
    "summary": "GET/PUT rules, templates, evaluate",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/automation/mailroom`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/automation/mailroom",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-014": {
    "summary": "conversations, failures, metrics, replay",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/automation/mailroom/metrics`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/automation/mailroom/metrics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SET-015": {
    "summary": "GET search",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/settings/automation/mailroom/search?q=test`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/settings/automation/mailroom/search?q=test",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-SRCH-001": {
    "summary": "Global search query",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/search`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/search",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-001": {
    "summary": "`/stats/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Stats"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-002": {
    "summary": "`/summary`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Home summary"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-003": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/POST`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/POST",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List/create"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-004": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET/PUT/DELETE`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET/PUT/DELETE",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "CRUD"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-005": {
    "summary": "`/:id/status`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PATCH`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PATCH",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Status transition"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-006": {
    "summary": "`/:id/subtasks/:subtaskId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Subtask toggle"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-007": {
    "summary": "`/:id/tags`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Tags"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-008": {
    "summary": "`/:id/comments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Comments + reactions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-009": {
    "summary": "`/:id/comment-attachments`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Attachment upload"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-010": {
    "summary": "`/:id/custom-fields`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Custom fields"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-011": {
    "summary": "`/:id/description-versions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Version history"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-012": {
    "summary": "`/:id/description-versions/restore`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/tasks/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Restore"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TASK-013": {
    "summary": "`/:id/activity-logs`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-001": {
    "summary": "Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/summary`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-002": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/types`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/types",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-003": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/targets/types/seed`.",
      "Assert response status is HTTP 200 or 201 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/targets/types/seed",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        201,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 201 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 201 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-TGT-004": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/lifecycle-options`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/lifecycle-options",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-005": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/leaderboard`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/leaderboard",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-006": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/platform-settings`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/platform-settings",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-007": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/targets/conflicts/check`.",
      "Assert response status is HTTP 200 or 400.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/targets/conflicts/check",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-API-TGT-008": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/targets/?limit=5`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/targets/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-009": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-010": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-011": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-012": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-013": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-014": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-015": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-016": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-017": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TGT-018": {
    "summary": "| TC-API-TGT-001–018 | /api/targets | Types, CRUD, activate/lock/complete/close, assignments, contributions, forecast, versions, redistribute, recalculate, leaderboard, platform-settings, conflicts |",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TRSH-001": {
    "summary": "`/stats`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Trash stats"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TRSH-002": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "List trashed records"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TRSH-003": {
    "summary": "`/:moduleKey/:recordId/restore`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated POST request to `/api/trash/people/__RECORD__/restore`.",
      "Paths with __RECORD__, __DEAL__, etc. resolve to first list item in tenant at runtime.",
      "Assert response status is HTTP 200 or 400 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "POST",
      "path": "/api/trash/people/__RECORD__/restore",
      "auth": "Bearer token (owner persona)",
      "body": "(minimal JSON `{}` or generator default)",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        404
      ],
      "behavior": "Restore"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TRSH-004": {
    "summary": "`/:moduleKey/:recordId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Permanent purge"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-TRSH-005": {
    "summary": "`/:moduleKey/:recordId`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Verify trash entry exists"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-001": {
    "summary": "`/registry`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Full bootstrap payload"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-002": {
    "summary": "`/apps`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Enabled apps"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-003": {
    "summary": "`/sidebar`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Navigation tree"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-004": {
    "summary": "`/routes`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Dynamic route definitions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-005": {
    "summary": "`/apps/:appKey/modules`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/ui/apps/SALES/modules`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/apps/SALES/modules",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Module list per app"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-006": {
    "summary": "`/entities`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/ui/entities`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/entities",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Entity registry"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-007": {
    "summary": "`/projection/:appKey/:moduleKey`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/ui/projection/SALES/people`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/projection/SALES/people",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create-form projection"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UI-008": {
    "summary": "`/app-definitions`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/ui/app-definitions`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/app-definitions",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "App definitions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-UPL-001": {
    "summary": "POST multipart upload",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/health/live`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/health/live",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Endpoint responds without server error; status matches smoke band (HTTP 200 or 400 or 403 or 404)."
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-001": {
    "summary": "`/profile`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Read own profile"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-002": {
    "summary": "`/profile`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `PUT`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "PUT",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update name, phone"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-003": {
    "summary": "`/profile/password`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Change password (valid old)"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-004": {
    "summary": "`/profile/password`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Wrong old password → 400"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-005": {
    "summary": "`/profile/avatar`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Upload avatar image"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-006": {
    "summary": "`/profile/avatar`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Remove avatar"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-007": {
    "summary": "`/list`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Assignment picker list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-008": {
    "summary": "`/add-capabilities`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Invite capabilities"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-009": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `GET`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "GET",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Admin list users"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-010": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Create user with role + appAccess"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-011": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Get user detail"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-012": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Update user role/apps"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-013": {
    "summary": "`/:id/reset-password`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Admin reset password"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-014": {
    "summary": "`/:id`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Deactivate user (not hard delete)"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-API-USER-015": {
    "summary": "`/`",
    "howToRun": [
      "Obtain owner JWT via login (ATP uses the owner persona).",
      "Send authenticated GET request to `/api/users/list`.",
      "Assert response status is HTTP 200 or 400 or 403 or 404.",
      "Smoke level: does not validate full response body shape."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/list",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Non-admin → 403"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 400, 403, 404], got <other>",
      "whatToFix": [
        "Endpoint must respond with status HTTP 200 or 400 or 403 or 404 for smoke pass.",
        "Wrong status usually means auth, routing, or missing tenant data."
      ],
      "likelyCauses": [
        "API down or `/health/ready` failing.",
        "Invalid/expired JWT or missing owner persona.",
        "Route not registered or wrong path after refactor.",
        "Missing seed record when path uses __RECORD__ / __DEAL__ placeholders."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs."
      ]
    }
  },
  "TC-ASYNC-001": {
    "summary": "POST `/api/communications/email`",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "POST `/api/communications/email`"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-002": {
    "summary": "Simulated failure",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Simulated failure"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-003": {
    "summary": "POST webhook",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "POST webhook"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-004": {
    "summary": "POST `/api/csv/import/*`",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "POST `/api/csv/import/*`"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-005": {
    "summary": "Import without Redis",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Import without Redis"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-010": {
    "summary": "ENABLE_DIGEST_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_DIGEST_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-011": {
    "summary": "same",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "same"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-012": {
    "summary": "ENABLE_ESCALATION_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_ESCALATION_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-013": {
    "summary": "ENABLE_TRASH_RETENTION_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_TRASH_RETENTION_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-014": {
    "summary": "ENABLE_ASSIGNMENT_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_ASSIGNMENT_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-015": {
    "summary": "ENABLE_PROCESS_WAIT_RESUME_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_PROCESS_WAIT_RESUME_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-016": {
    "summary": "ENABLE_DEFERRED_AUTOMATION_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_DEFERRED_AUTOMATION_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-017": {
    "summary": "ENABLE_BUSINESS_HOURS_KPI_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_BUSINESS_HOURS_KPI_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-018": {
    "summary": "ENABLE_TARGET_RECALC_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_TARGET_RECALC_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-019": {
    "summary": "ENABLE_HELPDESK_SLA_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_HELPDESK_SLA_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-020": {
    "summary": "ENABLE_GMAIL_INBOX_SYNC_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_GMAIL_INBOX_SYNC_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-021": {
    "summary": "ENABLE_GMAIL_PUSH",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_GMAIL_PUSH"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-022": {
    "summary": "ENABLE_SNOOZE_WAKE_NOTIFICATION_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_SNOOZE_WAKE_NOTIFICATION_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-023": {
    "summary": "ENABLE_APPOINTMENT_REMINDER_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_APPOINTMENT_REMINDER_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-024": {
    "summary": "ENABLE_QUOTE_EXPIRY_SCHEDULER",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "ENABLE_QUOTE_EXPIRY_SCHEDULER"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-030": {
    "summary": "Event received on trigger",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Event received on trigger"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-031": {
    "summary": "Inbox refresh event",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Inbox refresh event"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-032": {
    "summary": "Chat message delivered",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Chat message delivered"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-033": {
    "summary": "Agent receives customer message",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Agent receives customer message"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-034": {
    "summary": "Client reconnects after disconnect",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Client reconnects after disconnect"
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-040": {
    "summary": "One SLA notification per threshold per cycle (no duplicates)",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Worker/cron endpoint reachable; job may be accepted (202) or validated (400)."
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-041": {
    "summary": "User preference opt-out suppresses channel",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Worker/cron endpoint reachable; job may be accepted (202) or validated (400)."
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-ASYNC-042": {
    "summary": "Digest batches multiple events",
    "howToRun": [
      "Authenticate as owner.",
      "GET `/api/…` (async/cron/worker smoke).",
      "Expect HTTP 200 or 400 or 403 or 404.",
      "Import polling flows: see `async-import` suite and TC-ASYNC-004."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        403,
        404
      ],
      "behavior": "Worker/cron endpoint reachable; job may be accepted (202) or validated (400)."
    },
    "onFailure": {
      "typicalError": "Worker or cron endpoint not reachable",
      "whatToFix": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "likelyCauses": [
        "Background worker not running",
        "Mongo/queue unavailable",
        "Digest/import feature disabled"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "For import tests ensure CSV fixture and worker process on SUT."
      ]
    }
  },
  "TC-E2E-APT-001": {
    "summary": "Set availability → publish slug",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/appointments/config/me",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/appointments/config/me",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Public page loads"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-002": {
    "summary": "Select slot → book",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/public/book/atp-invalid-booking-slug/slots",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/public/book/atp-invalid-booking-slug/slots",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Appointment event created"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-003": {
    "summary": "Google OAuth → sync busy times",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/appointments/config/pages",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/appointments/config/pages",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Slots exclude busy"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-004": {
    "summary": "Team page → round-robin assign",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/appointments/config/teams",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/appointments/config/teams",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Correct assignee"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-005": {
    "summary": "Public manage token → reschedule",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/appointments/stats",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/appointments/stats",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Updated time"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-006": {
    "summary": "Public manage → cancel",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status cancelled"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-007": {
    "summary": "Book → advance time → reminder job",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Notification sent"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-APT-008": {
    "summary": "Mark no-show or complete",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status updated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-001": {
    "summary": "Create Internal Audit event → assign auditor",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "AUDIT_ASSIGNED"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-002": {
    "summary": "Plan audit beat multi-org route",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Events chain"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-003": {
    "summary": "POST execute check-in (geo if required)",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "AUDIT_CHECKED_IN; state checked_in"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-004": {
    "summary": "Complete form during execution → submit",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "AUDIT_SUBMITTED; needs_review"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-005": {
    "summary": "Findings → corrective action → due dates",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CORRECTIVE_ACTION_* events"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-006": {
    "summary": "Reviewer approve",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "AUDIT_APPROVED; closed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-007": {
    "summary": "Reject → re-submit",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "AUDIT_REJECTED → resubmit"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-008": {
    "summary": "Full flow → GET timeline",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Ordered events"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-009": {
    "summary": "Audit app offline queue → reconnect sync",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "No data loss"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUD-010": {
    "summary": "AUDIT context cases list",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Scoped to audit app"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-001": {
    "summary": "Create rule on CASE_CREATED → field update",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Side effect on create"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-002": {
    "summary": "Disable rule → trigger event",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "No side effect"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-003": {
    "summary": "POST preview with sample payload",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Preview result"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-004": {
    "summary": "Create process → test → webhook trigger",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Execution graph state"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-005": {
    "summary": "Process wait node → cron resume",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Continues execution"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-006": {
    "summary": "Submit for approval → approve in inbox",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Record unlocked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-007": {
    "summary": "Reject with comment",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Record returned"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-008": {
    "summary": "Run flow → check health/metrics",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Bottleneck detection"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-009": {
    "summary": "Off-hours trigger → deferred row → cron",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Executes in hours"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-AUTO-010": {
    "summary": "User rule on domain event → deliver",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "In-app + email per prefs"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-BOOT-001": {
    "summary": "Login → GET registry/sidebar/routes → dynamic routes registered",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "All entitled modules in sidebar"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-BOOT-002": {
    "summary": "Enable SALES+HELPDESK → switch apps in shell",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/apps",
      "2. GET /api/ui/sidebar",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/apps → /api/ui/sidebar",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Correct sidebar per app"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-BOOT-003": {
    "summary": "Login on terminated tenant",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/organization/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/organization/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Blocked at `/platform/home`"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-BOOT-004": {
    "summary": "Login → change role permissions → profile refresh",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "New permissions without re-login"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-001": {
    "summary": "Create item → add variant → price book entry",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/items?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/items?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Search finds variant"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-002": {
    "summary": "Configure bundle components → expand",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/catalog/price-books",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/catalog/price-books",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Correct component prices"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-003": {
    "summary": "Create category → assign attributes → assign item",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/catalog/categories",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/catalog/categories",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Filter by category"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-004": {
    "summary": "Upload images → reorder → delete",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/items?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/items?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Gallery state"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-005": {
    "summary": "Reduce stock below threshold",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/items/low-stock",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/items/low-stock",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Appears in low-stock"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CAT-006": {
    "summary": "link-deal from item record",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/deals/?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/deals/?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Relationship visible"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-001": {
    "summary": "Add mailbox → Gmail OAuth → sync",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/mailboxes?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Threads appear"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-002": {
    "summary": "Compose → send → thread created",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/communications/pipeline-metrics",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/communications/pipeline-metrics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Outbound in sent"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-003": {
    "summary": "Reply to thread → mark viewed",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/communications/templates",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/communications/templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Thread updated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-004": {
    "summary": "Snooze thread → scheduler fires",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/communications/inbound/diagnostics",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/communications/inbound/diagnostics",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "EMAIL_THREAD_SNOOZE_ENDED notification"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-005": {
    "summary": "POST create-task",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/tasks/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Task with relatedTo"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-006": {
    "summary": "POST create-case",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Case with email link"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-007": {
    "summary": "Connect SSE → send email → event received",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/inbox?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/inbox?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Real-time update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-008": {
    "summary": "Bounce → suppression list → block resend",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/communications/suppressions/stats",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/communications/suppressions/stats",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Suppression enforced"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-COM-009": {
    "summary": "Upload via communications",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/communications/inbound/dead-letter",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/communications/inbound/dead-letter",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Download works"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-001": {
    "summary": "Public POST demo → admin list",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "DemoRequest created"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-002": {
    "summary": "Approve demo → instance ACTIVE",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Dedicated DB if configured"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-003": {
    "summary": "PATCH status SUSPENDED",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Tenant blocked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-004": {
    "summary": "POST collect-all",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Metrics aggregated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-005": {
    "summary": "Platform admin update parser",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Test connection passes"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-CP-006": {
    "summary": "POST daily digest",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "DIGEST_DAILY notifications"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-001": {
    "summary": "Create form → sections/questions → save",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Builder persists"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-002": {
    "summary": "enable-public → submit via public slug",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/public/forms/atp-nonexistent-slug-404",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/public/forms/atp-nonexistent-slug-404",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Response created"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-003": {
    "summary": "Authenticated fill → submit",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Response linked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-004": {
    "summary": "Submit scored form → analytics",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "KPI computed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-005": {
    "summary": "Response → add corrective action → verify",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status transitions"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-006": {
    "summary": "Reviewer approve/reject",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Immutable after submit"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-007": {
    "summary": "link-event → submit during event execution",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Audit linkage"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-008": {
    "summary": "Export responses Excel + PDF report",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Files downloadable"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-FRM-009": {
    "summary": "Archive submitted response",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/forms?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/forms?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Hidden from active list"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-001": {
    "summary": "Create case → assign → priority",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. POST /api/helpdesk/cases",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CASE_CREATED notification"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-002": {
    "summary": "Inbound email via mailroom/webhook",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases/canned-responses",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/canned-responses",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Case auto-created; threaded"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-003": {
    "summary": "Reply with canned response merge tags",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Activity + email sent"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-004": {
    "summary": "Open → Pending → Resolved → Closed",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CASE_STATUS_CHANGED events"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-005": {
    "summary": "Closed → reopen",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CASE_REOPENED"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-006": {
    "summary": "Advance clocks → warning threshold",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/settings/applications/helpdesk/execution-settings",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/settings/applications/helpdesk/execution-settings",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "SLA_WARNING once per cycle"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-007": {
    "summary": "Pass resolution deadline",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases/analytics/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/analytics/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "SLA_BREACHED + escalation"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-008": {
    "summary": "Pause segment in business hours off",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases/analytics/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/analytics/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Clock paused"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-009": {
    "summary": "Rule matches → auto-assign",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases/analytics/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/analytics/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CASE_ASSIGNED"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-010": {
    "summary": "PATCH bulk status/priority",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases/analytics/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/analytics/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "All cases updated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-011": {
    "summary": "Seed cases → summary/trends/owners",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Non-zero metrics"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-012": {
    "summary": "Embed chat → case linked → agent sees messages",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "CASE_CHAT_MESSAGE_RECEIVED"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-HD-013": {
    "summary": "POST ingest/channel",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. POST /api/helpdesk/cases/ingest/channel",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases/ingest/channel",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Case from portal/chat channel"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-001": {
    "summary": "Upload CSV → map → check duplicates → import",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "ImportHistory success; rows in org"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-002": {
    "summary": "Same flow for deals",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/imports?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/imports?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Deals with correct stages"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-003": {
    "summary": "Import with duplicate emails",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. POST /api/csv/check-duplicates/contacts",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/csv/check-duplicates/contacts",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Skipped count in job"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-004": {
    "summary": "Invalid rows mid-batch",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/imports/stats/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/imports/stats/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Job status failed; no partial corrupt"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-005": {
    "summary": "Export people → re-import",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/csv/export/contacts",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/csv/export/contacts",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Data parity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-006": {
    "summary": "Import with Redis running",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/imports?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/imports?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Job processed by worker"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-IMP-007": {
    "summary": "Import without Redis",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/imports?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/imports?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Inline processing completes"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-001": {
    "summary": "Webhook MIME → mailroom → case adapter",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/webhooks/arivu/inbound-email/health",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/webhooks/arivu/inbound-email/health",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Conversation + case"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-002": {
    "summary": "Same thread second message",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/settings/automation/mailroom",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/settings/automation/mailroom",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Appended not duplicated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-003": {
    "summary": "Settings evaluate endpoint",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Routing decision logged"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-004": {
    "summary": "Simulated failure → replay",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/settings/automation/mailroom/failures?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/settings/automation/mailroom/failures?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Message reprocessed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-005": {
    "summary": "Bearer key ingest from external system",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. POST /api/public/mailroom/ingest",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/public/mailroom/ingest",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Conversation created"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-MRM-006": {
    "summary": "Inbound with attachments → download",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/settings/automation/mailroom",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/settings/automation/mailroom",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "File accessible"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-001": {
    "summary": "Login portal → audits list",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Only assigned"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-002": {
    "summary": "Open audit → read-only fields",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "No SALES data leak"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-003": {
    "summary": "Upload evidence on action",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "EVIDENCE_UPLOADED"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-004": {
    "summary": "POST case",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Case in helpdesk"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-005": {
    "summary": "Customer reply via portal",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Agent sees in helpdesk"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-006": {
    "summary": "Reply via mailroom path",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Thread continuity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-PRT-007": {
    "summary": "Update portal notification prefs",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/helpdesk/cases?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/helpdesk/cases?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Prefs saved"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-001": {
    "summary": "Create quote → add sections/lines from catalog",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Totals correct"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-002": {
    "summary": "Apply discount → recalculate",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/quotes?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/quotes?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Line + header totals"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-003": {
    "summary": "Submit → pending (locked) → approve",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/quotes?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/quotes?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status Approved"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-004": {
    "summary": "Submit → reject → revise",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/quotes?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/quotes?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "New revision unlocked"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-005": {
    "summary": "Share token → public view → accept",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status Accepted; activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-006": {
    "summary": "Share → reject with reason",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/public/quotes/atp-revoked/view",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/public/quotes/atp-revoked/view",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status Rejected"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-007": {
    "summary": "Generate document → download",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/quotes?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/quotes?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "PDF valid"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-008": {
    "summary": "Approved quote → convert",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/quotes?limit=1",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/quotes?limit=1",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Deal created; conversion link"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-009": {
    "summary": "Set expiry → run scheduler",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. POST /api/digest/trigger/daily",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/digest/trigger/daily",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status Expired"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-QTE-010": {
    "summary": "High-value quote → process engine approval",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/approvals?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/approvals?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "ApprovalInstance resolved"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-RPT-001": {
    "summary": "Create report → run → export",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Data matches filters"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-001": {
    "summary": "Create person (Lead) → assign owner → tag",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "participations.SALES.lead_status"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-002": {
    "summary": "Update lead score → activity log",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Activity entry"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-003": {
    "summary": "POST convert-lead-to-contact",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Role=Contact; lifecycle event"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-004": {
    "summary": "Link person to company account",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Relationship instance"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-005": {
    "summary": "Create deal linked to person + account",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "dealPeople, dealOrganizations"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-006": {
    "summary": "PATCH stage through pipeline → Won",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Stage history; probability"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-007": {
    "summary": "Move to Lost stage",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/deals/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status + activity"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-008": {
    "summary": "Create related task from deal record",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "relatedTo link"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-009": {
    "summary": "Create event linked to deal",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/events/?limit=5",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/events/?limit=5",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Event on calendar"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-010": {
    "summary": "Configure rule → create unassigned lead → auto-assign",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/settings/automation/assignment-rules",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/settings/automation/assignment-rules",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "PEOPLE_ASSIGNED notification"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SLS-011": {
    "summary": "Import sales-lifecycle template → trigger stage",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/admin/business-flow-templates",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Flow metrics update"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SRCH-001": {
    "summary": "Cmd+K search person/deal/case",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Results tenant-scoped"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-SRCH-002": {
    "summary": "Cmd+/ create person drawer",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/ui/registry",
      "2. GET /api/users/profile",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/ui/registry → /api/users/profile",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Record created"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-001": {
    "summary": "Wizard → assign users → activate",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Target active"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-002": {
    "summary": "Deal won → contribution ledger",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Progress updated"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-003": {
    "summary": "Run TARGET_RECALC",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Totals refreshed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-004": {
    "summary": "Lifecycle transitions",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Status enforced"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-005": {
    "summary": "Multiple contributors → GET leaderboard",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Rankings correct"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TGT-006": {
    "summary": "Overlapping targets → check",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/targets/summary",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/targets/summary",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Conflict reported"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-001": {
    "summary": "DELETE person/deal/case/task/event/item/org/quote",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "deletedAt set; in trash"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-002": {
    "summary": "Delete org with linked deals",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "blocked + dependencies"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-003": {
    "summary": "Restore from trash",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Record active"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-004": {
    "summary": "Purge from trash",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Gone from DB"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-005": {
    "summary": "Past retention → purge job",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Expired snapshots removed"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-E2E-TRSH-006": {
    "summary": "Hold flag → retention cron",
    "howToRun": [
      "E2E catalog flow — ATP proxy uses API steps (not full Playwright journey).",
      "1. GET /api/trash/",
      "Each step asserts HTTP status in the allowed band.",
      "For full UI journeys see suites `e2e-critical`, `e2e-sales`, etc."
    ],
    "request": {
      "method": "MULTI",
      "path": "/api/trash/",
      "auth": "Bearer token (owner persona)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": "per-step smoke band",
      "behavior": "Not purged"
    },
    "onFailure": {
      "typicalError": "Expected one of [200, 201, …], got 4xx/5xx on a step",
      "whatToFix": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "likelyCauses": [
        "Upstream data missing for POST steps (e.g. helpdesk case create).",
        "Feature flag or module disabled in tenant.",
        "API contract changed vs catalog."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-001": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/health/ready",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-002": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/health/live",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-003": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/health/status",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-004": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/people/?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-005": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/deals/?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-006": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/organization/?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-007": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/tasks/?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-008": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/registry",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-009": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/sidebar",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-010": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/users/profile",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-011": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/search/?q=test&limit=10",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-012": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-013": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/events/?limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-014": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/notifications?appKey=SALES&limit=20",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-015": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/apps",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-016": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/admin/automation-rules?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-017": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/admin/business-flow-templates",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-018": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/admin/business-flows?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-019": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/admin/processes?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-020": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/appointments/config",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-021": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/appointments/config/me",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-022": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/appointments/config/pages",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-023": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/appointments/config/slug-available?slug=atp-test",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-024": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/appointments/stats",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-025": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/audit/assignments?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-026": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/business-hours/holiday-calendars",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-027": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/business-hours/kpis",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-028": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/business-hours/sets",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-029": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/catalog/categories",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-030": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/catalog/price-books",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-031": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/inbound/dead-letter",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-032": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/inbound/diagnostics",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-033": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/templates",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-034": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/webhook-test/events",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-035": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/workspace-thread-counts",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-036": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/workspace-thread-ids",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-037": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/workspace-threads",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-038": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/config-registry/lifecycle-status-mappings/people_lifecycle",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-039": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/config-registry/lifecycles/people",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-040": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/config-registry/pipelines/deals/stages",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-041": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/deals/?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-042": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/events/export",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-043": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/forms/responses/all?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-044": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/health/live",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-045": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/analytics/audit-export",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-046": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/analytics/distribution",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-047": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/analytics/owners",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-048": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/canned-responses",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-049": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/items/low-stock",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-050": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/items/statistics",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-051": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/items/type/product",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-052": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/mailboxes?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-053": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-054": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/quick-create",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-055": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/notifications?appKey=SALES&limit=10",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-056": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/people/?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-057": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/portal/cases?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-058": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/push/subscriptions",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-059": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/reports?limit=1",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-060": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/reports?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-061": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/applications",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-062": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/applications/helpdesk/execution-settings",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-063": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/automation/assignment-rules",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-064": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/automation/mailroom",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-065": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/automation/mailroom/metrics",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-066": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/automation/mailroom/search?q=test",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-067": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/core-modules/organizations/status-types",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-068": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/core-modules/people/people-types",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-069": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/integrations",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-070": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/organization",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-071": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/quotes",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-072": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/security",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-073": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/subscriptions",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-074": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-075": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/leaderboard",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-076": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/lifecycle-options",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-077": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/platform-settings",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-078": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/summary",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-079": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/types",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-080": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/tasks/?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-081": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/app-definitions",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-082": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/apps/SALES/modules",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-083": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/entities",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-084": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/ui/projection/SALES/people",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-085": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/users/list",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-086": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/v2/organization/?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M001": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/business-hours/simulate",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M002": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/ingest/channel",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M003": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/catalog/price-books/resolve",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M004": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/config-registry/compute-derived-status",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M005": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/csv/check-duplicates/contacts",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M006": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/events/",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M007": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/events/bulk-delete",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M008": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/records/batch",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M009": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/settings/applications/helpdesk/recalculate-slas",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M010": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/types/seed",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-M011": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/targets/conflicts/check",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P001": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/__CASE__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P002": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/__CASE__/chat/session",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P003": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/__CASE__/chat/messages",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P004": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/helpdesk/cases/__CASE__/chat/stream",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P005": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/email/compose-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P006": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/communications/email/reply-to-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P007": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/events/__EVENT__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P008": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/forms/__FORM__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P009": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/forms/__FORM__/analytics",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P010": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/forms/__FORM__/kpis",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P011": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/forms/__FORM__/responses?limit=5",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P012": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/records/__RECORD__/activity",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P013": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/records/__RECORD__/comments",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P014": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/records/__RECORD__/neighbors",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P015": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/modules/people/records/__RECORD__/description-versions",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P016": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/quotes/__QUOTE__/process-approvals",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P017": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/quotes/__QUOTE__/conversion",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P018": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/quotes/__QUOTE__/sections",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P019": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/quotes/__QUOTE__/lines",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P020": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/quotes/__QUOTE__/documents",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P021": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/relationships/links?moduleKey=people&recordId=__RECORD__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P022": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/relationships/linkable-targets?moduleKey=deals&recordId=__RECORD__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P023": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/relationships/record-context?moduleKey=people&recordId=__RECORD__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P024": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/scheduling/deals/__DEAL__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-P025": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/scheduling/__SCH__",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-LOAD-U003": {
    "summary": "Concurrent load",
    "howToRun": [
      "Concurrent load: 10 VUs for 30s (env ATP_LOAD_VUS, ATP_LOAD_DURATION_SEC).",
      "Do not run in PR — use nightly or manual against UAT.",
      "Unset ATP_SKIP_LOAD_TESTS to execute."
    ],
    "request": {
      "method": "LOAD",
      "path": "/api/auth/test-version",
      "auth": "varies",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "Error rate ≤ 0.05, p95 ≤ 3000ms"
    },
    "onFailure": {
      "typicalError": "p95 or error rate exceeded load thresholds",
      "whatToFix": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "likelyCauses": [
        "DB pool exhaustion",
        "CPU saturation",
        "rate limiting"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-001": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/health/ready",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-002": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/health/live",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-003": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/health/status",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-004": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/people/?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-005": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/deals/?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-006": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/organization/?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-007": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/tasks/?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-008": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/registry",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-009": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/sidebar",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-010": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/users/profile",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-011": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/search/?q=test&limit=10",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-012": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-013": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/events/?limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-014": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/notifications?appKey=SALES&limit=20",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-015": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/apps",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-016": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/admin/automation-rules?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-017": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/admin/business-flow-templates",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-018": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/admin/business-flows?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-019": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/admin/processes?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-020": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/appointments/config",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-021": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/appointments/config/me",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-022": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/appointments/config/pages",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-023": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/appointments/config/slug-available?slug=atp-test",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-024": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/appointments/stats",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-025": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/audit/assignments?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-026": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/business-hours/holiday-calendars",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-027": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/business-hours/kpis",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-028": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/business-hours/sets",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-029": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/catalog/categories",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-030": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/catalog/price-books",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-031": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/inbound/dead-letter",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-032": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/inbound/diagnostics",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-033": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/templates",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-034": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/webhook-test/events",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-035": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/workspace-thread-counts",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-036": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/workspace-thread-ids",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-037": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/workspace-threads",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-038": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/config-registry/lifecycle-status-mappings/people_lifecycle",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-039": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/config-registry/lifecycles/people",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-040": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/config-registry/pipelines/deals/stages",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-041": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-042": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/events/export",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-043": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/forms/responses/all?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-044": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/health/live",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-045": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/analytics/audit-export",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-046": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/analytics/distribution",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-047": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/analytics/owners",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-048": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/canned-responses",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-049": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/items/low-stock",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-050": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/items/statistics",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-051": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/items/type/product",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-052": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/mailboxes?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-053": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-054": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/quick-create",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-055": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/notifications?appKey=SALES&limit=10",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-056": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/people/?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-057": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/portal/cases?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-058": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/push/subscriptions",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-059": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/reports?limit=1",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-060": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/reports?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-061": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/applications",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-062": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/applications/helpdesk/execution-settings",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-063": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/automation/assignment-rules",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-064": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/automation/mailroom",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-065": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/automation/mailroom/metrics",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-066": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/automation/mailroom/search?q=test",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-067": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/core-modules/organizations/status-types",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-068": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/core-modules/people/people-types",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-069": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/integrations",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-070": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/organization",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-071": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/quotes",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-072": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/security",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-073": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/subscriptions",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-074": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-075": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/leaderboard",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-076": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/lifecycle-options",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-077": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/platform-settings",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-078": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/summary",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-079": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/types",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-080": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/tasks/?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-081": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/app-definitions",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-082": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/apps/SALES/modules",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-083": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/entities",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-084": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/ui/projection/SALES/people",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-085": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/users/list",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-086": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/v2/organization/?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M001": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/business-hours/simulate",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M002": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/ingest/channel",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M003": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/catalog/price-books/resolve",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M004": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/config-registry/compute-derived-status",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M005": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/csv/check-duplicates/contacts",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M006": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/events/",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M007": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/events/bulk-delete",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M008": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/records/batch",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M009": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/settings/applications/helpdesk/recalculate-slas",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M010": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/types/seed",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-M011": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/targets/conflicts/check",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P001": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/__CASE__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P002": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/__CASE__/chat/session",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P003": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/__CASE__/chat/messages",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P004": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/helpdesk/cases/__CASE__/chat/stream",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P005": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/email/compose-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P006": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/communications/email/reply-to-preview?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P007": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/events/__EVENT__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P008": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/forms/__FORM__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P009": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/forms/__FORM__/analytics",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P010": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/forms/__FORM__/kpis",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P011": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/forms/__FORM__/responses?limit=5",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P012": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/records/__RECORD__/activity",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P013": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/records/__RECORD__/comments",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P014": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/records/__RECORD__/neighbors",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P015": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/modules/people/records/__RECORD__/description-versions",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P016": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/quotes/__QUOTE__/process-approvals",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P017": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/quotes/__QUOTE__/conversion",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P018": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/quotes/__QUOTE__/sections",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P019": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/quotes/__QUOTE__/lines",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P020": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/quotes/__QUOTE__/documents",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P021": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/relationships/links?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P022": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/relationships/linkable-targets?moduleKey=deals&recordId=__RECORD__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P023": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/relationships/record-context?moduleKey=people&recordId=__RECORD__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P024": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/scheduling/deals/__DEAL__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-P025": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/scheduling/__SCH__",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PERF-U003": {
    "summary": "Sequential latency",
    "howToRun": [
      "Sequential samples: 20 (warmup 2).",
      "Measures p50/p95/p99 for single-user latency SLA."
    ],
    "request": {
      "method": "PERF",
      "path": "/api/auth/test-version",
      "auth": "Bearer (owner)",
      "body": null
    },
    "expected": {
      "status": "metrics",
      "behavior": "p95 ≤ 800ms, p99 ≤ 1500ms"
    },
    "onFailure": {
      "typicalError": "Latency percentile above perf threshold",
      "whatToFix": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "likelyCauses": [
        "Slow query",
        "cold cache",
        "local dev machine noise"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`."
      ]
    }
  },
  "TC-PUB-APT-001": {
    "summary": "Manage page",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/appointments/manage/atp-invalid-token`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/appointments/manage/atp-invalid-token",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Manage page"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-APT-002": {
    "summary": "Reschedule slots",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/appointments/manage/atp-invalid-token/slots`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/appointments/manage/atp-invalid-token/slots",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Reschedule slots"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-APT-003": {
    "summary": "Reschedule",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/appointments/manage/atp-invalid-token/reschedule`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/appointments/manage/atp-invalid-token/reschedule",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Reschedule"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-APT-004": {
    "summary": "Cancel",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/appointments/manage/atp-invalid-token/cancel`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/appointments/manage/atp-invalid-token/cancel",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Cancel"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-APT-005": {
    "summary": "404",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "404"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-BOOK-001": {
    "summary": "Booking page config",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/book/atp-invalid-booking-slug`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/book/atp-invalid-booking-slug",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Booking page config"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-BOOK-002": {
    "summary": "Available slots",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Available slots"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-BOOK-003": {
    "summary": "Book appointment",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/book/atp-invalid-booking-slug/book`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/book/atp-invalid-booking-slug/book",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Book appointment"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-001": {
    "summary": "Widget config",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Widget config"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-002": {
    "summary": "Create session",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/embed/chat/sessions`.",
      "Include headers: {\"X-Instance-Key\":\"atp-invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/embed/chat/sessions",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "X-Instance-Key": "atp-invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Create session"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-003": {
    "summary": "Message exchange",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/embed/chat/messages`.",
      "Include headers: {\"X-Instance-Key\":\"atp-invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/embed/chat/messages",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "X-Instance-Key": "atp-invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Message exchange"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-004": {
    "summary": "Real-time messages",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/embed/chat/stream`.",
      "Include headers: {\"X-Instance-Key\":\"atp-invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/embed/chat/stream",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "X-Instance-Key": "atp-invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Real-time messages"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-005": {
    "summary": "Presence",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/embed/chat/typing`.",
      "Include headers: {\"X-Instance-Key\":\"atp-invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/embed/chat/typing",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "X-Instance-Key": "atp-invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Presence"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-006": {
    "summary": "End chat",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/embed/chat/close`.",
      "Include headers: {\"X-Instance-Key\":\"atp-invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/embed/chat/close",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "X-Instance-Key": "atp-invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "End chat"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-CHAT-007": {
    "summary": "401",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-FRM-001": {
    "summary": "Load public form",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/forms/atp-nonexistent-slug-404`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/forms/atp-nonexistent-slug-404",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Load public form"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-FRM-002": {
    "summary": "Submit response",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/forms/atp-nonexistent-slug-404/submit`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/forms/atp-nonexistent-slug-404/submit",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Submit response"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-FRM-003": {
    "summary": "404",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "404"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-MRM-001": {
    "summary": "Inbound with Bearer key",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/mailroom/ingest`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/mailroom/ingest",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Inbound with Bearer key"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-MRM-002": {
    "summary": "Thread continuity",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/mailroom/ingest`.",
      "Body: {\"threadId\":\"atp-thread-1\",\"messageId\":\"atp-msg-2\",\"subject\":\"ATP thread append\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/mailroom/ingest",
      "auth": "None (public)",
      "body": {
        "threadId": "atp-thread-1",
        "messageId": "atp-msg-2",
        "subject": "ATP thread append"
      },
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Thread continuity"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-MRM-003": {
    "summary": "401",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/mailroom/ingest`.",
      "Include headers: {\"Authorization\":\"Bearer invalid\"}",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/mailroom/ingest",
      "auth": "Bearer / ingest key (see headers)",
      "body": "{}",
      "headers": {
        "Authorization": "Bearer invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-001": {
    "summary": "View quote",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "View quote"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-002": {
    "summary": "Download PDF",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/quotes/atp-revoked-token-xyz/pdf`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/quotes/atp-revoked-token-xyz/pdf",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Download PDF"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-003": {
    "summary": "Accept quote",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/quotes/atp-revoked-token-xyz/accept`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/quotes/atp-revoked-token-xyz/accept",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Accept quote"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-004": {
    "summary": "Reject quote",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/public/quotes/atp-revoked-token-xyz/reject`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/quotes/atp-revoked-token-xyz/reject",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Reject quote"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-005": {
    "summary": "Portal comments",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/quotes/atp-revoked-token-xyz/comments`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/quotes/atp-revoked-token-xyz/comments",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Portal comments"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-006": {
    "summary": "404",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "404"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-QTE-007": {
    "summary": "429",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/quotes/atp-revoked-token-xyz/view`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/quotes/atp-revoked-token-xyz/view",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-001": {
    "summary": "HMAC-valid inbound",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "HMAC-valid inbound"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-002": {
    "summary": "401",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send GET `/api/public/…`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/…",
      "auth": "None (public)",
      "body": null,
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-003": {
    "summary": "MIME inbound",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/webhooks/email/inbound`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/webhooks/email/inbound",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "MIME inbound"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-004": {
    "summary": "SES bounce/complaint",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/webhooks/email/ses-events`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/webhooks/email/ses-events",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "SES bounce/complaint"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-005": {
    "summary": "Provider events",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/webhooks/email/events`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/webhooks/email/events",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Provider events"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-006": {
    "summary": "Gmail push notification",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/webhooks/email/gmail/push`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/webhooks/email/gmail/push",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Gmail push notification"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-007": {
    "summary": "Process webhook trigger",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/hooks/process/atp-invalid-webhook-key`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/hooks/process/atp-invalid-webhook-key",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Process webhook trigger"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-PUB-WH-008": {
    "summary": "401",
    "howToRun": [
      "No user JWT — call public/embed/webhook endpoint directly.",
      "Send POST `/api/hooks/process/atp-invalid-webhook-key`.",
      "Assert status in [200, 400, 401, 403, 404, 429, 503]."
    ],
    "request": {
      "method": "POST",
      "path": "/api/hooks/process/atp-invalid-webhook-key",
      "auth": "None (public)",
      "body": "{}",
      "headers": {
        "Content-Type": "application/json"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Unexpected HTTP status outside public smoke band",
      "whatToFix": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "likelyCauses": [
        "Webhook signature or ingest key required but not configured in fixtures.",
        "Rate limiting (429) or service unavailable (503).",
        "Public route path changed."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Optional tokens: copy `fixtures/public.example.json` → `fixtures/public.json`."
      ]
    }
  },
  "TC-SEC-APP-001": {
    "summary": "403 APP_ENTITLEMENT_REQUIRED",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/deals/?limit=5`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "403 APP_ENTITLEMENT_REQUIRED"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-002": {
    "summary": "403 APP_NOT_ENABLED",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/deals/?limit=5`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "403 APP_NOT_ENABLED"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-003": {
    "summary": "402 TRIAL_EXPIRED",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/users/profile`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/profile",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "402 TRIAL_EXPIRED"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-004": {
    "summary": "Redirect `/audit/dashboard`",
    "howToRun": [
      "Log in as owner via Playwright.",
      "Open `/audit/dashboard`.",
      "Verify page loads under entitlement/RBAC expectations (smoke)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/dashboard",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "Redirect `/audit/dashboard`"
    },
    "onFailure": {
      "typicalError": "Configure viewer persona / redirected or forbidden UI",
      "whatToFix": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "likelyCauses": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "howToFix": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ],
      "remediation": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ]
    }
  },
  "TC-SEC-APP-005": {
    "summary": "Redirect `/portal/dashboard`",
    "howToRun": [
      "Log in as owner via Playwright.",
      "Open `/portal/dashboard`.",
      "Verify page loads under entitlement/RBAC expectations (smoke)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/dashboard",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "Redirect `/portal/dashboard`"
    },
    "onFailure": {
      "typicalError": "Configure viewer persona / redirected or forbidden UI",
      "whatToFix": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "likelyCauses": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "howToFix": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ],
      "remediation": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ]
    }
  },
  "TC-SEC-APP-006": {
    "summary": "INTERNAL_INSTANCE_OVERRIDE bypass",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/users/profile`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/profile",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "INTERNAL_INSTANCE_OVERRIDE bypass"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-007": {
    "summary": "200 list",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/deals/?limit=5`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "200 list"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-008": {
    "summary": "403 EXECUTION_SEAT_REQUIRED",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "403 EXECUTION_SEAT_REQUIRED"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-009": {
    "summary": "403 or tab hidden",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/users/profile`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/users/profile",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "403 or tab hidden"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-APP-010": {
    "summary": "403 USER_TYPE_NOT_ALLOWED",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/deals/?limit=5`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/deals/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "403 USER_TYPE_NOT_ALLOWED"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-CSRF-001": {
    "summary": "403 CSRF_INVALID_ORIGIN",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "403 CSRF_INVALID_ORIGIN"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-CSRF-002": {
    "summary": "200 (JWT path)",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "200 (JWT path)"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-001": {
    "summary": "404 or 403",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "404 or 403"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-002": {
    "summary": "Ignored/rejected",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "Ignored/rejected"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-003": {
    "summary": "Proxy routing",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/organization/`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/organization/",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "Proxy routing"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-004": {
    "summary": "No cross-DB bleed",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/organization/`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/organization/",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "No cross-DB bleed"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-005": {
    "summary": "organizationId isolation",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/imports/stats/summary`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/imports/stats/summary",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "organizationId isolation"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-006": {
    "summary": "404",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/public/quotes/atp-revoked/view`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/public/quotes/atp-revoked/view",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "404"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-007": {
    "summary": "No cross-instance session",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/embed/chat/config`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/embed/chat/config",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": {
        "X-Instance-Key": "invalid"
      }
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "No cross-instance session"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-008": {
    "summary": "401",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "POST `/api/public/mailroom/ingest`.",
      "Assert HTTP 401 or 403 or 404."
    ],
    "request": {
      "method": "POST",
      "path": "/api/public/mailroom/ingest",
      "auth": "Bearer token (owner)",
      "body": "{}",
      "headers": null
    },
    "expected": {
      "status": [
        401,
        403,
        404
      ],
      "behavior": "401"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-009": {
    "summary": "Filter",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "Filter"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-MT-010": {
    "summary": "No foreign records",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "No foreign records"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-001": {
    "summary": "403",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "403"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-002": {
    "summary": "Filter applied",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "Filter applied"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-003": {
    "summary": "Filter applied",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/people/?limit=5`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "Filter applied"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-004": {
    "summary": "No ownership filter",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "No ownership filter"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-005": {
    "summary": "200",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/…`.",
      "Assert HTTP 200 or 403 or 404."
    ],
    "request": {
      "method": "GET",
      "path": "/api/…",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        403,
        404
      ],
      "behavior": "200"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-006": {
    "summary": "403 without",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/people/?limit=5`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "403 without"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-007": {
    "summary": "403 without",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/people/?limit=5`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/people/?limit=5",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "403 without"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RBAC-008": {
    "summary": "No button / disabled",
    "howToRun": [
      "Log in as viewer via Playwright.",
      "Open `/deals`.",
      "Verify page loads under entitlement/RBAC expectations (smoke)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/deals",
      "auth": "UI session (viewer)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "No button / disabled"
    },
    "onFailure": {
      "typicalError": "Configure viewer persona / redirected or forbidden UI",
      "whatToFix": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "likelyCauses": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "howToFix": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ],
      "remediation": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ]
    }
  },
  "TC-SEC-RBAC-009": {
    "summary": "Toast/error",
    "howToRun": [
      "Log in as viewer via Playwright.",
      "Open `/deals`.",
      "Verify page loads under entitlement/RBAC expectations (smoke)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/deals",
      "auth": "UI session (viewer)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "Toast/error"
    },
    "onFailure": {
      "typicalError": "Configure viewer persona / redirected or forbidden UI",
      "whatToFix": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "likelyCauses": [
        "Viewer persona missing",
        "Viewer has owner-level access (test may skip in full security suite)"
      ],
      "howToFix": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ],
      "remediation": [
        "Add restricted viewer to fixtures/personas.json.",
        "Run `npm run run:security` for deep RBAC assertions."
      ]
    }
  },
  "TC-SEC-RBAC-010": {
    "summary": "Matrix applied",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/roles/`.",
      "Assert HTTP 200."
    ],
    "request": {
      "method": "GET",
      "path": "/api/roles/",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200
      ],
      "behavior": "Matrix applied"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-001": {
    "summary": "429 after threshold",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429 after threshold"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-002": {
    "summary": "429",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-003": {
    "summary": "429",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-004": {
    "summary": "429",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-005": {
    "summary": "Requests proceed (logged)",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "Requests proceed (logged)"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-SEC-RL-006": {
    "summary": "429 at threshold",
    "howToRun": [
      "Call with owner (or cross-tenant) JWT.",
      "GET `/api/ui/registry`.",
      "Assert HTTP 200 or 400 or 401 or 403 or 404 or 429 or 503."
    ],
    "request": {
      "method": "GET",
      "path": "/api/ui/registry",
      "auth": "Bearer token (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": [
        200,
        400,
        401,
        403,
        404,
        429,
        503
      ],
      "behavior": "429 at threshold"
    },
    "onFailure": {
      "typicalError": "Unexpected status for security scenario",
      "whatToFix": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "likelyCauses": [
        "Tenant isolation bug if 200 on cross-tenant ID",
        "RBAC not enforced"
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Configure a restricted `viewer` persona if testing RBAC denial.",
        "Seed data may be missing — ensure the org has sample records for placeholder IDs.",
        "Escalate to security suite owner — do not ignore 200 on negative tests."
      ]
    }
  },
  "TC-UI-APT-001": {
    "summary": "Booking pages list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/pages` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/pages",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-APT-002": {
    "summary": "Personal booking config",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/configure` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/configure",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-APT-003": {
    "summary": "Admin configure user",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/configure/user/000000000000000000000002` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/configure/user/000000000000000000000002",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-APT-004": {
    "summary": "New team page",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/team/configure` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/team/configure",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-APT-005": {
    "summary": "Edit team page",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/team/configure/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/team/configure/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-APT-006": {
    "summary": "Google/Microsoft OAuth start",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-001": {
    "summary": "Stats; upcoming audits",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/dashboard` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/dashboard",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-002": {
    "summary": "Schedule audit/beat",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/schedule` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/schedule",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-003": {
    "summary": "Audit list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/audits` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/audits",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-004": {
    "summary": "Execution detail",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/audits/:eventId` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/audits/:eventId",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-005": {
    "summary": "Findings list (GenericModule)",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/findings` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/findings",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-006": {
    "summary": "Response list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/responses` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/responses",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-007": {
    "summary": "Response detail",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/audit/forms/000000000000000000000001/responses/:responseId` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/audit/forms/000000000000000000000001/responses/:responseId",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-008": {
    "summary": "Queue when offline",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUD-009": {
    "summary": "Redirect",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/deals` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/deals",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUTH-001": {
    "summary": "`/login` → valid login → redirect to saved route or platform home",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUTH-002": {
    "summary": "`/login` invalid credentials",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUTH-003": {
    "summary": "Protected route without auth → `/login` + `arivu_redirect_after_login`",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUTH-004": {
    "summary": "Logout clears token, redirects to login",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-AUTH-005": {
    "summary": "Cold start: login → dynamic routes registered from `/api/ui/registry`",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-CP-001": {
    "summary": "Control plane home",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/control` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/control",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-CP-002": {
    "summary": "Demo queue",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/control/demo-requests` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/control/demo-requests",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-CP-003": {
    "summary": "Instance list/status",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/control/instances` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/control/instances",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-CP-004": {
    "summary": "Parser configuration",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/control/inbound-parser` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/control/inbound-parser",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-001": {
    "summary": "New module enabled server-side → appears in sidebar after refresh",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-002": {
    "summary": "`{routeBase}` list renders GenericModule",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-003": {
    "summary": "`{routeBase}/new` opens create drawer",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-004": {
    "summary": "`{routeBase}/:id` opens ModuleRecordPage",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-005": {
    "summary": "Disabled module → route not registered",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-DYN-006": {
    "summary": "CONTROL_PLANE routes never injected for tenant",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-001": {
    "summary": "Case list; filters; analytics link",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/helpdesk/cases` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/helpdesk/cases",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-002": {
    "summary": "Create case drawer",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/helpdesk/cases/new` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/helpdesk/cases/new",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-003": {
    "summary": "Email timeline; reply; canned responses; SLA badge",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/helpdesk/cases/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/helpdesk/cases/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-004": {
    "summary": "Resolve with resolution code",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-005": {
    "summary": "Agent chat with embed session",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-006": {
    "summary": "Add/remove participants",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-HD-007": {
    "summary": "Side thread panel",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-OBS-001": {
    "summary": "Event received",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-OBS-002": {
    "summary": "Events tracked",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-001": {
    "summary": "App cards; attention items; quick links",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-002": {
    "summary": "App registry; enable/disable (admin)",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-003": {
    "summary": "Attention surface items",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/platform/attention` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/platform/attention",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-004": {
    "summary": "Empty state; connect mailbox; thread list/read/reply/compose",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-005": {
    "summary": "Pending list; empty state",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-006": {
    "summary": "Approve/reject actions",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/approvals/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/approvals/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-007": {
    "summary": "List; restore; permanent delete",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-008": {
    "summary": "Collapse; app switch; module navigation",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-009": {
    "summary": "Open record in tab; close tab; dirty guard",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-010": {
    "summary": "Unread count; mark read; SSE update",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-011": {
    "summary": "Search records; navigate to result",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-012": {
    "summary": "Create person/org/task/event commands",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PLT-013": {
    "summary": "Light/dark toggle persists",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-001": {
    "summary": "Summary cards",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/dashboard` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/dashboard",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-002": {
    "summary": "Audit list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/audits` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/audits",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-003": {
    "summary": "Audit detail",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/audits/:eventId` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/audits/:eventId",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-004": {
    "summary": "Corrective actions",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/actions` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/actions",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-005": {
    "summary": "Case list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/cases` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/cases",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-006": {
    "summary": "Case detail + reply",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/cases/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/cases/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-007": {
    "summary": "Profile edit",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/portal/profile` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/portal/profile",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PRT-008": {
    "summary": "Navigate all portal tabs",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-001": {
    "summary": "Valid/invalid login; redirect; remember route",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-002": {
    "summary": "Submit demo request form",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-003": {
    "summary": "Render form; validation; submit success/error",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/public/atp-slug` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/public/atp-slug",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-004": {
    "summary": "View; accept; reject; comments; PDF download",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/public/quotes/atp-token` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/public/quotes/atp-token",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-005": {
    "summary": "Slot picker; book; confirmation",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/book/atp-slug` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/book/atp-slug",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-006": {
    "summary": "Embedded booking iframe",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/book/atp-slug/embed` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/book/atp-slug/embed",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-007": {
    "summary": "Reschedule; cancel; invalid token",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/appointments/manage/atp-token` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/appointments/manage/atp-token",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-PUB-008": {
    "summary": "Open widget; send message; receive SSE",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-001": {
    "summary": "Edit profile; password; avatar",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=profile` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=profile",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-002": {
    "summary": "Org name; logo; address",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=organization` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=organization",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-003": {
    "summary": "Create set; holidays; simulate",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=business-hours` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=business-hours",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-004": {
    "summary": "Users CRUD; roles; groups",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=users-access` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=users-access",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-005": {
    "summary": "Fields; layouts per module",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=core-modules&moduleKey=*` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=core-modules&moduleKey=*",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-006": {
    "summary": "Pipelines; schema; permissions",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=applications&app=sales` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=applications&app=sales",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-007": {
    "summary": "Case schema; SLA; analytics",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=applications&app=helpdesk` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=applications&app=helpdesk",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-008": {
    "summary": "Enable/disable apps",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=applications&view=management` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=applications&view=management",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-009": {
    "summary": "Assignment rules CRUD",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=automation&automationView=assignment-rules` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=automation&automationView=assignment-rules",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-010": {
    "summary": "Mailroom rules",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=automation&automationView=mailroom` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=automation&automationView=mailroom",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-011": {
    "summary": "Catalog settings",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=automation&automationView=catalog` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=automation&automationView=catalog",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-012": {
    "summary": "Quote settings",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=automation&automationView=quotes` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=automation&automationView=quotes",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-013": {
    "summary": "Full-page rules",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/automation-rules` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/automation-rules",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-014": {
    "summary": "Process list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/processes` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/processes",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-015": {
    "summary": "New process designer",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/processes/new` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/processes/new",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-016": {
    "summary": "Edit process graph",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/processes/000000000000000000000001/design` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/processes/000000000000000000000001/design",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-017": {
    "summary": "Business flows list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/flows` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/flows",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-018": {
    "summary": "Create flow",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/flows/create` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/flows/create",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-019": {
    "summary": "Flow health dashboard",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/automation/flows/000000000000000000000001/health` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/automation/flows/000000000000000000000001/health",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-020": {
    "summary": "Targets management",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=performance&view=targets` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=performance&view=targets",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-021": {
    "summary": "Performance dashboards",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=performance&view=dashboards` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=performance&view=dashboards",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-022": {
    "summary": "Plan view; upgrade",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=subscriptions` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=subscriptions",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-023": {
    "summary": "Prefs; channels; rules; health",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=notifications&notificationPage=*` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=notifications&notificationPage=*",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-024": {
    "summary": "Security settings; activity log",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=security` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=security",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-025": {
    "summary": "Email; calendar; webhooks",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=integrations&integrationView=*` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=integrations&integrationView=*",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-026": {
    "summary": "Demo list (master org dev)",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=demo-requests` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=demo-requests",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-027": {
    "summary": "Instance management",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings?tab=instances` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings?tab=instances",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-028": {
    "summary": "User without settings sees only entitled tabs",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-029": {
    "summary": "Notification overview page",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/notifications/overview` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/notifications/overview",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-030": {
    "summary": "Notification rules page",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/notifications/rules` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/notifications/rules",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SET-031": {
    "summary": "Admin health (admin only)",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/settings/notifications/health` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/settings/notifications/health",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-001": {
    "summary": "Widgets load; metrics non-error",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-002": {
    "summary": "List; filter; sort; bulk actions",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-003": {
    "summary": "Create lead/contact; validation",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-004": {
    "summary": "Profile; participations; activity; link org",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-005": {
    "summary": "List; filters",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-006": {
    "summary": "Create company account",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-007": {
    "summary": "Detail; related people/deals",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-008": {
    "summary": "Edit drawer/page",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/organizations/000000000000000000000001/edit` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/organizations/000000000000000000000001/edit",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-009": {
    "summary": "Kanban view; drag stage; list view toggle",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-010": {
    "summary": "Deal record; stage history; related; comments",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-011": {
    "summary": "List; mark complete; filters",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-012": {
    "summary": "Subtasks; comments; related record",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-013": {
    "summary": "Calendar/list; filters",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/events` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/events",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-014": {
    "summary": "Create meeting/audit/beat",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/events/create` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/events/create",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-015": {
    "summary": "Event detail",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/events/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/events/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-016": {
    "summary": "Execution surface only",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/events/000000000000000000000001/execute` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/events/000000000000000000000001/execute",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-017": {
    "summary": "Catalog list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/items` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/items",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-018": {
    "summary": "Variants; media; pricing sections",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/items/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/items/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-019": {
    "summary": "List; create drawer",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/quotes` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/quotes",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-020": {
    "summary": "Full create flow",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/quotes/new` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/quotes/new",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-021": {
    "summary": "Lines; sections; approval; share",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/quotes/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/quotes/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-022": {
    "summary": "Forms hub",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-023": {
    "summary": "Form creation wizard",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/create` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/create",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-024": {
    "summary": "Drag-drop builder; save",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/builder/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/builder/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-025": {
    "summary": "Form settings",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/000000000000000000000001/detail` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/000000000000000000000001/detail",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-026": {
    "summary": "Response list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/000000000000000000000001/responses` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/000000000000000000000001/responses",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-027": {
    "summary": "Response review",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/000000000000000000000001/responses/:responseId` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/000000000000000000000001/responses/:responseId",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-028": {
    "summary": "Internal fill",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/forms/000000000000000000000001/fill` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/forms/000000000000000000000001/fill",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-029": {
    "summary": "Global responses",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/responses` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/responses",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-030": {
    "summary": "Import list",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/imports` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/imports",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-031": {
    "summary": "Import progress/detail",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/imports/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/imports/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-032": {
    "summary": "Groups list",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-033": {
    "summary": "Members; activity",
    "howToRun": [
      "This catalog row has no automatable route in the title.",
      "Marked skipped at runtime — implement a hand-written Playwright flow or extend the UI generator."
    ],
    "request": {
      "method": "BROWSER",
      "path": "(not automated)",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "skipped",
      "behavior": "Manual UI scenario — not executed by generated smoke."
    },
    "onFailure": {
      "typicalError": "No automatable route — manual UI scenario",
      "whatToFix": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "likelyCauses": [
        "Title uses dynamic path (`:id`) or settings query not mapped by generator."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-034": {
    "summary": "Target wizard",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/targets/new` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/targets/new",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  },
  "TC-UI-SLS-035": {
    "summary": "Target progress",
    "howToRun": [
      "Playwright opens Chromium (headless unless `ATP_UI_HEADLESS=0`).",
      "Log in via UI using owner persona credentials.",
      "Navigate to client route `/targets/000000000000000000000001` (sidebar or goto).",
      "Wait for `domcontentloaded` and verify main content is visible (not login redirect)."
    ],
    "request": {
      "method": "BROWSER",
      "path": "/targets/000000000000000000000001",
      "auth": "UI session (owner)",
      "body": null,
      "headers": null
    },
    "expected": {
      "status": "page_loaded",
      "behavior": "SPA loads target module; user remains authenticated; main layout renders."
    },
    "onFailure": {
      "typicalError": "Redirected to login / main content not found",
      "whatToFix": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "likelyCauses": [
        "Client not running on `ATP_SUT_CLIENT_URL`.",
        "Login failed — check personas.json.",
        "Route renamed in Vue router.",
        "Permission or entitlement hides module."
      ],
      "howToFix": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ],
      "remediation": [
        "Confirm SUT API is running (`ATP_SUT_API_URL`, default http://localhost:3000).",
        "Verify owner credentials in `fixtures/personas.json` or `ATP_PERSONA_OWNER_*`.",
        "Confirm client is running (`ATP_SUT_CLIENT_URL`, default http://localhost:5173).",
        "Run `npm run playwright:install` if the browser is missing."
      ]
    }
  }
}
