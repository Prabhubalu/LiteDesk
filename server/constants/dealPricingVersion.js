/**
 * Pricing engine version stamped on each DealLine.
 * Bump when tax/discount/currency rules change so totals can be re-run intentionally.
 */
const CURRENT_DEAL_PRICING_VERSION = 1;

module.exports = {
  CURRENT_DEAL_PRICING_VERSION
};
