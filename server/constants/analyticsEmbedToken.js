const ANALYTICS_EMBED_TOKEN_PREFIX = 'ld_embed_';
const ANALYTICS_EMBED_TOKEN_STATUSES = Object.freeze(['active', 'revoked']);

module.exports = {
  ANALYTICS_EMBED_TOKEN_PREFIX,
  ANALYTICS_EMBED_TOKEN_STATUSES,
};
