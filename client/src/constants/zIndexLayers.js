/** Quick preview drawer (list record peek). */
export const Z_INDEX_QUICK_PREVIEW_DRAWER = 9999;

/** Modal / full-screen drawer dialogs. */
export const Z_INDEX_MODAL = 10000;

/** Body-teleported popovers/tooltips above quick preview and modals. */
export const Z_INDEX_FLOATING_OVERLAY = 10001;

/** Tailwind class — literal string required for JIT. */
export const FLOATING_OVERLAY_Z_CLASS = 'z-[10001]';
