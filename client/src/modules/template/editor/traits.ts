import type { Editor } from 'grapesjs';

/** Phase 2: custom traits for merge fields, conditions, print settings. */
export function registerTraits(_editor: Editor): void {
  // Reserved for property inspector integration
}
